@page
@model ProjektInzynierski.Pages.Exam.ExamInfoModel
@using TestTest.Models.Db;
@using Microsoft.AspNetCore.Mvc;
@using Microsoft.AspNetCore.Identity
@inject SignInManager<Person> SignInManager
@inject UserManager<Person> UserManager


@{
    ViewData["Title"] = "Informacje dotyczące testu";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Podsumowanie testu</h1>

<div>
    <h4>Test został automatycznie oceniony, poniżej możesz zobaczyć liczbę uzyskanych punktów oraz ocenę.</h4>
    <p>Jeśli chcesz zobaczyć poprawne odpowiedzi, przejdź do szczegółowego podsumowania.</p>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Test.Title):
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Test.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Test.Description):
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Test.Description)
        </dd>
        <dt class="col-sm-2">
            Liczba punktów:
        </dt>
        <dd class="col-sm-10">
            @{
                var maxPunkt = Model.Test.Questions.Count();
                var liczbaPunktow = Model.Result.Points % 1 != 0 ? $"{Model.Result.Points:G2}" : $"{Model.Result.Points}";
                liczbaPunktow += $"/{maxPunkt.ToString()} pkt.";
                Console.WriteLine(liczbaPunktow);
            }
            @Html.Raw(liczbaPunktow)
        </dd>
        <dt class="col-sm-2">
            Ocena:
        </dt>
        <dd class="col-sm-10">
            <text>@Html.Raw($"{Model.Marks.First()}")</text>
        </dd>

    </dl>
</div>
<div>
    @if (User.IsInRole("Uczen") && @Model.Test.IsVisible== false)
    {}
    else
    {
        <a class="btn btn-standard" asp-page="./ExamDetailedInfo" asp-route-id="@Model.Result.Id">Zobacz szczegóły</a>
    }
    @if (SignInManager.IsSignedIn(User))
    {
        var userId = UserManager.GetUserId(User);
        var user = await UserManager.FindByIdAsync(userId);

        @if ((User.IsInRole("Nauczyciel")) || (User.IsInRole("Admin")))
        {
            <a class="btn btn-back" asp-page="./Details" asp-route-id="@Model.OriginalTestId">Powrót</a>
        }
        else
        {
            <a class="btn btn-back ml-2" asp-page="./Marks">Powrót</a>
        }
    }
</div>