@page
@model ProjektInzynierski.Pages.Exam.ExamPerformModel

@{
    ViewData["Title"] = "Rozwiąż test";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1 class="display-4">Rozwiąż test</h1>

<div>
    <div id="timer" class="mb-3"></div>
    <form id="examForm" method="post">
        <table class="table text1">
            <tbody>
                @for (int i = 0; i < Model.testQuestions.Count; i++)
                {
                    var question = Model.testQuestions[i];
                    <tr>
                        <td>
                            <strong>Pytanie @(i + 1):</strong> @question.Text
                        </td>
                    </tr>
                    <tr>
                        <td>
                            @if (question.TypeId == ViewBag.idWielokrotnego)
                            {
                                foreach (var answer in question.Answers)
                                {
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" name="@answer.QuestionId" value="@answer.Id">
                                        <label class="form-check-label">
                                            @answer.Text
                                        </label>
                                    </div>
                                }
                            }
                            else
                            {
                                foreach (var answer in question.Answers)
                                {
                                    <div class="form-check">
                                        <input class="form-check-input" type="radio" name="@answer.QuestionId" value="@answer.Id">
                                        <label class="form-check-label">
                                            @answer.Text
                                        </label>
                                    </div>
                                }
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <input type="submit" value="Zapisz" class="btn btn-standard" />
    </form>
</div>

@section Scripts {
    <script>
        //var countdown = @Model.Duration; //czas trwania w sekundach (test)
        var countdown = @Model.Duration * 60;
        var timerElement = document.getElementById('timer');

        function updateTimer() {
            var minutes = Math.floor(countdown / 60);
            var seconds = countdown % 60;

            timerElement.textContent = 'Czas pozostały: ' + minutes + 'min ' + seconds + 's';

            if (countdown <= 0) {
                alert('Czas testu minął.');
                document.getElementById('examForm').submit(); //automatyczne zapisanie odpowiedzi po zakończeniu czasu
            } else {
                countdown--;
                setTimeout(updateTimer, 1000);
            }
        }

        updateTimer();
    </script>
}
